# A mixed stationary profile repairs the calibration table

| Status | Provenance | Lean | Resolution |
| --- | --- | --- | --- |
| `PROVED`, maturity `M+L+C` | exact rational calibration in the group derivation | checked through full-rate stationary cap interface | `MINED` regression |

In the calibrated three-player table, player one quits surely while players
zero and two mix with hazards `1/2` and `1/4`. The prescribed payoff and every
player's exact arbitrary-behavior cap both equal `(1,3/4,1/2)`. Thus the profile
is exact terminal Nash and supplies a uniform-equilibrium payoff.

The example is intentionally small: it falsifies the inference from failure of
several natural endpoint grammars to dynamic necessity. Its exact payoff/cap
equalities are the permanent positive regression for stationary search.
